<%= simple_form_for(@meeting_location) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.association :meeting %>
    <%= f.association :location %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
