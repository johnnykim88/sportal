<h1>Listing user_meetings</h1>

<table>
  <tr>
    <th>User</th>
    <th>Meeting</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @user_meetings.each do |user_meeting| %>
  <tr>
    <td><%= user_meeting.user %></td>
    <td><%= user_meeting.meeting %></td>
    <td><%= link_to 'Show', user_meeting %></td>
    <td><%= link_to 'Edit', edit_user_meeting_path(user_meeting) %></td>
    <td><%= link_to 'Destroy', user_meeting, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New User meeting', new_user_meeting_path %>
